// Moved to @ladigital/admin-kit (shared package). Re-exported here so existing
// imports keep working.
export {
  inputBase,
  labelBase,
  primaryButton,
  TextField,
  NumberField,
  TextArea,
  Toggle,
  AdminPageHeader,
  SaveButton,
  Card,
} from "@ladigital/admin-kit";
