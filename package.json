{
  "name": "ladigital-monorepo",
  "private": true,
  "version": "0.0.0",
  "packageManager": "npm@11.7.0",
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "build": "turbo run build",
    "dev": "turbo run dev",
    "lint": "turbo run lint",
    "ladigital": "npm run dev -w la-digital",
    "build:ladigital": "npm run build -w la-digital"
  },
  "devDependencies": {
    "turbo": "^2.3.3"
  }
}
